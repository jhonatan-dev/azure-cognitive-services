<div class="card border-0 shadow my-5">
    <div class="card-body p-5">
        <h1 class="font-weight-light">Text Analytics API</h1>
        <form id="form-keyword-extraction">
            <div class="form-group">
                <label for="text-area-keyword-extraction">Example textarea</label>
                <textarea class="form-control" id="text-area-keyword-extraction" rows="3" required minlength="31"
                    maxlength="5120"></textarea>
                <small class="form-text text-muted">Nota: Solo se permite texto de más de 30 caracteres.</small>
            </div>
            <button type="submit" class="btn btn-primary">Enviar</button>
        </form>
    </div>
</div>

<div class="card border-0 shadow my-5">
    <div class="card-body p-5">
        <h1 class="font-weight-light">Text Analytics API</h1>
        <p class="lead">
            In this snippet, the background image is fixed to the body element.
            Content on the page will scroll, but the image will remain in a
            fixed position!
        </p>
        <p class="lead">Scroll down...</p>
        <div style="height: 700px;"></div>
        <p class="lead mb-0">You've reached the end!</p>
    </div>
</div>